{
  "name": "jufexk",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": "1.3.14"
  },
  "scripts": {
    "dev": "wrangler dev",
    "build": "vite build",
    "test": "vitest run",
    "deploy": "bun run build && wrangler deploy",
    "types": "wrangler types",
    "check": "wrangler types && tsc --noEmit && vitest run && vite build && wrangler deploy --dry-run",
    "db:local": "wrangler d1 migrations apply jufexk --local"
  },
  "dependencies": {
    "hono": "4.12.32"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.18.8",
    "@types/node": "26.1.2",
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.1.10",
    "wrangler": "4.114.0"
  }
}
