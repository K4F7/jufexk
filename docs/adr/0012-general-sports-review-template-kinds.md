# 统一普通课程评价模板，并将修读属性移出课程分类

课程对学生是必修还是选修取决于培养方案，不能成为课程的全局属性；站内分类仅用于为任课关系选择评价问题集，因此统一为 `general | sports`，只有明确体育性质证据选择 `sports`，其余均选择 `general`。来源中的必修、限选、任选、选修等原始文字继续作为溯源证据保留，不进入业务分类；未来课程特点通过独立、多值的课程标签表达，不扩展或嵌套评价模板类型。
