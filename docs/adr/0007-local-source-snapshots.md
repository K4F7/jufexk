# 原始教务快照仅保存在本地受控存储

一次性目录基线采集为每个查询页保留原始 GBK HTML、脱敏请求参数和 SHA-256，以便解析缺陷出现时离线复核与重新派生，而无需再次访问教务系统。来源快照必须位于 Git 忽略的本机受控目录，不得包含账号密码、Cookie 或会话令牌，也不得写入业务数据库；正式派生数据仅保留课程—教师覆盖及来源定位所需的最小字段。活跃工作副本保留到基线导入、历史评价映射完成并稳定运行 90 天，随后压缩加密转入受控离线长期归档；仓库仅永久保留非敏感覆盖摘要和内容哈希。该选择以受控保存内部课表快照为代价，换取一次性采集的可审计性与可恢复性。
