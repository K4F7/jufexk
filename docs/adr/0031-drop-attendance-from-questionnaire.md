# 问卷去掉考勤松紧：v4 回到四道三档题

状态：已接受

日期：2026-08-25

修订 [ADR-0027](./0027-attendance-tier-v3.md)：六个规则键各追加 v4。最新公共核回到四道三档题（课程难度、作业多少、给分好坏、收获多少），不再出 `attendance` **考勤松紧**。

- 改规则只追加版本：已发布的 v3（四维加考勤）只读、不回填、不改写。公开条目仍按各自快照翻译——v2 / v4 行四个标签，v3 行五个标签，缺考勤的旧快照不伪造。
- 新提交写入 v4。线下课与带 `mooc` 标签的课题目相同；`offlineOnly` 机制保留，但 v4 最新题集不用它。提交再带 `attendance` 键一律拒绝。
- v4 与 v2 / v3 一样 `averagesDimensions: false`：四维方向不一致，禁止收成维度均分 Chip；关系级 / 目录级聚合继续只看这四维。
