# 公开面加载速度总结 — 2026-08-30

未登录实测生产 **https://courses.sein.moe**。目录 **11167** 条 / **559** 页。

## 读数约定

用户都在大陆。本探针从 Cloudflare `IAD` 打，墙钟含 250–300ms 海外 RTT，**不当作用户指标**。

- **认：** Worker `Server-Timing` 的 `app` / `query`（#720 起才有）。大陆用户付的是这笔时间加国内边缘 RTT。
- **不认：** 本探针 HTTP 墙钟、Playwright「可见就绪」里的海外跳。
- 基线（#720 前）没有 `app`。当时慢路径墙钟 0.9–1.8s，源站占大头，表里用冷墙钟近似源站。

三轮同一套 `pnpm run timing:prod-public`，不改生产 UI。

| 轮次 | 生产时点 | 报告 |
| --- | --- | --- |
| 基线 | 01:26Z，#720 前 | [load-speed-2026-08-30.md](./load-speed-2026-08-30.md) |
| 第一轮 | 04:19Z，[#720](https://github.com/K4F7/jxufe-course-review/pull/720) | [load-speed-2026-08-30-after-opt.md](./load-speed-2026-08-30-after-opt.md) |
| 第二轮 | 12:59Z，[#734](https://github.com/K4F7/jxufe-course-review/pull/734)，`remote-SIN` | [load-speed-2026-08-30-after-round2.md](./load-speed-2026-08-30-after-round2.md) |
| 2026-08-31 | 11:30Z，#734 之后的课评流缓存等，`local-EWR` | [load-speed-2026-08-31.md](./load-speed-2026-08-31.md) |

## 两轮优化做了什么

**#720（#705）** — 预计算不再挡读；详情 / 点评第一页 / `config` 进 `s-maxage=60`；游客共享缓存；浏览器 30s 内存缓存；`Server-Timing`。

**#734（#723）** — 对准第一轮复测点名的尾巴：课号快路径、去掉窗口 `COUNT(*)`、体育虚拟行并页、点评认可投影、普通课程列表允许 `jufexk_voter`、admin session 按需探测。另有 #732 首屏 JS 拆分；首页改到 `/latest`。

## 源站对照（毫秒）

基线列为冷墙钟（近似源站）。#720 / #734 列为 Worker `app`。

| 场景 | 基线 | #720 | #734 | 相对基线 |
| --- | ---: | ---: | ---: | ---: |
| 目录首页 | ~1700 | 164 | 244 | 秒级 → 0.2s |
| 搜索 线性代数 / 孙爱琳 /「微」 | 1360–1480 | 66–148 | 97–135 | 秒级 → ~0.1s |
| 搜索课号 | ~1250 | **1264** | **105** | **-92%（第二轮）** |
| 通识第一页 | ~1210 | **1023** | **163** | **-84%（第二轮）** |
| 数学 / 思政 / 英语 | 990–1125 | 47–57 | 51–79 | 秒级 → &lt;80ms |
| 体育第一页 | ~1470 | **1132** | **104** | **-91%（第二轮）** |
| 全部第 2 页 | ~1770 | 121 | 137 | 秒级 → ~0.13s |
| 最新课评 | — | 14 | 22 | 已经够快 |
| 课程详情 | ~1080 | 48 | 61 | 秒级 → ~60ms |
| 点评「认可最多」 | ~830 | **664** | **45** | **-93%（第二轮）** |
| 点评 5 星 / 1 星 | ~550 | 443 / 503 | **36 / 40** | **-92%（第二轮）** |
| `/api/config` | ~260 | 14 | 9 | 热路径边缘 HIT |
| `/api/admin/session` | — | 0 | 0 | 不是体验问题 |

可缓存接口热路径边缘 HIT 约 **10ms**（大陆边缘同样量级）。

体育在 #720 复测时只剩 2 条 / 1 页；#734 后恢复 **37** 条 / **2** 页，第 2 页源站 91ms，不是空页假加速。

## 结论

公开面冷路径从 **0.9–1.8s 源站** 收到 **大多 50–160ms**。

第一轮吃掉「预计算挡读」和「详情/点评不进缓存」。第二轮吃掉课号、通识、体育、认可排序这几条秒级尾巴。`/latest` 16–25ms、admin session 0ms，对大陆用户不必再为它们改。

本探针 `/latest` ~300ms、admin 401 ~270ms 是美东 RTT，**不是用户体验，也不排优先级**。

## 2026-08-31 复测

同一套脚本。Worker 全程 `local-EWR` / `ORD` / `MIA`，`app` 回到 1.0–1.6s。热路径仍是 10ms HIT。
这是美东本地 Worker 打 D1 的距离，**不是 #734 SQL 被回退**（课号快路径、stale 预计算仍在主干）。
大陆用户继续以 #734 `remote-SIN` 的 50–160ms 为源站参照。

新变化：`/latest` 已可缓存（冷 MISS → 热 HIT）。关系列表带 `jufexk_voter` 仍 BYPASS。

## 还剩什么

秒级源站（在靠近 D1 的 placement 下）已经没有了。还值得做的产品改动只有一件：

**关系列表忽略 `jufexk_voter`。** 课表 / 搜索 / pill 走 `view=relations`。游客点过点评就会带这颗 Cookie，关系列表被挡在共享缓存外，每次回源 50–160ms。匿名 payload 不带 `viewer_*`（只登录用户才有）。#734 已对普通课程列表放行，关系列表还没有。放行后回访会变成边缘 HIT（约 10ms）。

其余不必当优化任务：

- 通识 163ms、首页 244ms、普通搜索 97–135ms 是现在最慢的源站，再抠收益远小于上面那次缓存放行。
- 首页 244ms 比 #720 的 164ms 略高，当查询改写波动，先不追。
- 思政 pill 浏览器 1.5s 对不上同出口 HTTP（74–184ms），不当回归。
- 计时脚本的 `sort=rating_desc` 和点评 Select 对不上现网，只影响复测。
