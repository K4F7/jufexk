## Parent

- 领域词：`CONTEXT.md`（批准数据包、历史评价生产导入、目录身份映射）
- 规格：ADR 0001、ADR 0017、ADR 0020
- 前置：#316（八路审核包已关）、#318（人审与批准包 v5）
- 输入包：`scripts/legacy_evidence/output/review-approved-20260820-v5/`（契约 `legacy-review-approved-package-v1`，evaluations **630**，SHA-256 `27ba8bff846bb74b77728ccf23075a193385c9d01157c77fea785d4ee04bdfae`）
- 生产导入 runbook：`docs/historical-production-import.md`
- **不要**写进 #200
- **不要**重放已导入批次：#108 的 522、#111 的 164/120/12、体育课名×老师的 64

## 目标

把 v5 批准包编成**可注入的生产候选冻结包**，做只读预检。默认停在预检。所有者在本票明确授权 `--apply` 之前，不得写生产 D1。

1. 校验 v5 manifest / JSONL 哈希，正文权威仍是公式栏，不得另写一稿。
2. 将 `course` / `teacher` 标签映射到现行目录基线身份（课号、`sourceTeacherLabel`、任课关系）。映射不上的不得猜测，进目录增补请求或排除报告。
3. 缺任课关系时只走 **append-only 新增**（ADR 0017），不得改课、改师、删关系。
4. 编出版本化生产候选冻结包（新目录，gitignored）：`importable-legacy-reviews.jsonl` + 排除/待补关系清单 + 证据哈希。不得覆盖 `full-matrix-freeze-20260819-v1`、#316 各路 `out_dir`、v5 批准包、既有 `jufexk-production-inputs` 里已导入批次。
5. 在维护窗口工具上对候选包做**只读预检**（退出码表示预览完成且没有写入）。把预检计数、与现场目录 marker 的差异写回本票。

## 已定决策

1. 输入只认 v5，不把 v1/v3/v4 混进同一冻结包。
2. 所有者已裁定且必须保留：`10|N` 课名英语口语；第 173 行孙爱琳；第 180 行缪丽。`56|J` 已删除，不入库。
3. 不发明第三版通顺稿。
4. 不写腾讯表格、不把证据纳入 Git。
5. 不得重放 522 / 164 / 120 / 12 / 64。
6. `--apply` 必须另一次所有者评论授权，并使用尚不存在的新备份路径。本票创建时未授权写入。

## 验收

- [ ] v5 哈希核验通过后再映射
- [ ] 生产候选包每条可追溯到 v5 `key`，正文 SHA 与公式栏一致
- [ ] 目录映射有终态：已绑定 / 待补任课 / 排除，无未决猜测
- [ ] 只读预检零写入；现场课程/教师/任课计数与公开历史评价数记录在本票
- [ ] 未覆盖受保护目录；未写 #200
- [ ] 未执行 `--apply`，除非本票出现明确授权评论

## 非目标

- 不重跑 OCR / A/B / 图文核验
- 不改冻结矩阵包
- 不在未授权时执行 `historical-import:production` 或任何生产 D1 写入
