# DESIGN.md — 选课志

工具优先。学生带着「这门课谁教得好」的问题来，扫读效率就是产品力。

## 入口页与工具页

站点分两种页面：

- **入口页**（根路径首屏）：slogan「关于一门课，上过的人最清楚。」大字衬线独占，配一个搜索框——搜索或点击即进入工具页。slogan 是全站唯一的衬线与装饰元素，只出现在这里。
- **工具页**（目录、详情、问卷、后台）：纯工具，高密度，无 slogan、无品牌块、无 hero 横幅、无徽章贴纸；页眉只有导航。

## 字体

- 界面与正文：系统无衬线（`--sans`），15px / 1.55；
- 入口页 slogan：衬线斜体（`--serif`）；
- 课号、评分、计数：等宽数字（tabular-nums），右对齐。

## 色板

- 基础：纸 `--paper`、墨 `--ink`、弱化 `--muted`、边线 `--line`；
- 强调 `--accent`（红）：只用于主操作按钮和评分数字；
- 状态：`--green` 通过、`--red` 驳回；
- 明暗两套跟随系统，语义不变。

## 版式规则

- 公开侧列表一律表格化：列对齐、行高不超过 48px、整行可点、悬停整行高亮；后台暂不纳入本轮收口，后续单独处理；
- 桌面端每屏可见至少 15 行数据；
- 筛选器是一条与表格同宽的紧凑工具栏，不独占竖向空间；
- 留白只用于分组，不用于气氛。

## 禁止

- 超过 32px 的展示型大标题（入口页 slogan 除外）；
- 装饰性图形、旋转元素、多层阴影；
- 任何为「气质」牺牲一行数据的排版。
