# DESIGN.md — 选课志

工具优先。学生带着「这门课由谁来教、大家如何评价这位教师教这门课」的问题来，筛选与比较效率就是产品力。本站辅助选课，但不承担排课或课表规划。

## 主界面与工具页

根路径直接进入选课决策台，不设置独立品牌入口页：

- **选课决策台**（根路径）：以任课关系（课程 × 教师）及其评价为核心，支持高密度筛选与比较；开课班只补充学期、班次、校区和时间上下文，不成为评分或标签的聚合边界。进入网站即可查课评课，不经过品牌首屏。
- **其他工具页**（详情、问卷、后台）：与选课决策台使用同一套纯工具语言，无 slogan、无品牌块、无 hero 横幅、无徽章贴纸；页眉只保留导航与任务所需操作。
- 公开一级导航固定为 `课评 / 课程目录 / 写评价`。`课评`指向根路径；教师通过任课关系搜索和详情进入，不设置独立一级教师列表；管理后台使用独立地址，不出现在公开主导航中。

## 信息层级

- 主列表一行对应一个任课关系，以“教师 × 课程”的顺序呈现；同一教师教授不同课程时是不同的评价对象。
- 进入任课关系详情后，评分、评价标签、结构化维度和学生投稿都只聚合该任课关系的数据，不产生教师全局总分。
- 开课班与学期只作为学生投稿的来源上下文展示，不拆分或合并任课关系。
- 任课关系详情采用类似评论区的连续阅读形态，但它不是社交讨论区：公开内容全部来自既有问卷中审核通过的学生投稿，不提供就地评论、回复、楼中楼或点赞。
- 课程目录只展示课号、课程名称、院系、课程标签等权威事实，以及任课教师数和学生投稿数；不得把不同教师的评分聚合为“课程总评分”。课程详情列出该课程下各任课关系的独立评分与评价入口。

## 主界面结构

- 桌面端采用双栏主从布局。左栏约占 35%，容纳搜索、筛选与高密度“教师 × 课程”任课关系列表；右栏约占 65%，展示当前任课关系摘要与学生投稿流。
- 切换左栏对象时右栏原位更新，当前任课关系同步进入 URL，支持刷新、前进后退和分享。
- 根路径没有任课关系参数时不自动选中目录第一项，也不默认推荐教师；右栏保持简洁的未选择状态。URL 指定任课关系时直接打开对应详情。
- 移动端不保留双栏：先显示任课关系列表，选择后进入独立详情，并提供明确返回路径。
- 左栏无搜索词时按课程课号、课程名称、教师名称的稳定目录顺序排列；搜索时按匹配相关度排列。评分和评价数可以由用户主动选择排序，但不作为默认排行榜。
- 左栏每个任课关系使用约 48–52px 的紧凑两行数据行：第一行“教师 × 课程”，右侧为评分；第二行显示课号与院系，右侧为评价数。它保持列对齐、分隔线和整行点击，不使用独立卡片。
- 左栏常驻统一搜索（教师名、课程名、课号）、院系、是否已有评价与排序；课程标签筛选保留常驻位置，但具体标签体系另行确定。
- 点击任课关系中的教师姓名时，左栏切换为该教师全部任课关系的可清除筛选，不创建教师全局总评页；点击课程名称时进入权威课程详情。

## 学生投稿流

- 右栏采用单页连续滚动，不设置“概览 / 学生评价 / 历史资料”等内容标签页。顺序固定为：任课关系紧凑摘要、排序与学期筛选、学生投稿流、独立的历史文字资料区块。
- 任课关系摘要同时展示平均推荐度、有效学生投稿数、5 至 1 分分布和已填写结构化维度的平均值；不以单个平均数代替样本量与分歧程度。
- 任何数量的审核通过学生投稿都正常公开并进入真实分布。有效投稿少于 5 条时标记“样本较少”；用户按评分排序时，这些任课关系排在达到 5 条门槛的对象之后。不得伪造平滑分数或隐藏真实投稿。
- 默认按投稿所填学期从新到旧排列；未填写或无法比较学期时，以审核公开时间从新到旧兜底。
- 用户可以主动切换为总体推荐度从高到低或从低到高。
- 单条学生投稿始终显示总体推荐度与学期，并以紧凑数据带展示问卷中实际填写的全部结构化维度；未填写字段直接省略，不显示“未提及”占位。
- 文字正文超过约四行时默认折叠并提供“展开全文”。投稿只标记为匿名，不展示虚构头像、昵称或其他身份线索。
- 历史评价只在末尾独立、默认折叠的“历史文字资料”区块展示，不混入学生投稿流，也不参与评分、评价数量、排序或其他聚合。

## 投稿问卷

- 保留四段向导：①课程与任课教师；②总体推荐度与结构化维度；③课堂、考核与文字补充；④完整预览、匿名说明与提交。
- 桌面端可以在当前步骤旁显示进度和已填答案摘要；移动端使用底部固定的“上一步 / 下一步”。步骤语义在不同断点保持一致。
- 问卷优先降低对象选错与漏填风险，不套用目录页的极限信息密度要求。
- 总体推荐度使用五个直接点选的文字选项：`1 不推荐`、`2 较不推荐`、`3 看情况`、`4 推荐`、`5 强烈推荐`；不使用下拉框或五星图标。
- 投稿采用双入口：从任课关系详情进入时自动带入当前教师与课程，第一步显示对象摘要并允许“更改”；全局导航保留从零查找对象的“写评价”入口。
- 第一步提供“找不到课程或教师”的目录补充申请入口。用户提交的是待审核申请，不直接创建公开目录对象；随附评价按 ADR-0002 暂存并在目录对象获批后进入评价审核队列。
- 问卷内选择“找不到课程或教师”后，第一步原位切换为目录补充申请模式；用户填写缺失对象与来源说明后继续同一个四段流程，最终一次提交申请和随附评价，无需跳页或日后重填。
- 页面底部继续提供不附评价的独立目录补充申请入口。
- 问卷草稿只在当前浏览器标签页会话内自动保存：刷新、切步骤或页面内返回时恢复，关闭标签页或提交成功后清除。从另一任课关系进入时必须让用户选择继续旧草稿或重新开始，不得静默覆盖评价对象。

## 字体

- 界面与正文：系统无衬线（`--sans`），15px / 1.55；
- 课号、评分、计数：等宽数字（tabular-nums），右对齐。

## 色板

- 基础：黑、白与蓝灰中性色；白色承载主要数据面，蓝灰用于页面底色、弱化文字、分隔线与次级控件；
- 强调 `--accent`（蓝）：只用于当前选择、主操作和可交互状态，不用大面积品牌色块；
- 状态：`--green` 通过、`--red` 驳回；
- 首轮仅设计浅色主题，不提供深色模式或主题切换器。

## 视觉基线

- 采用数据工具式极简：蓝灰页面底色、白色数据面、冷灰 1px 分隔线，复杂度来自信息层级、对齐和状态反馈。
- 圆角控制在 4–6px；常规页面与数据面不使用阴影，只有真正脱离文档流的浮层可以使用单层弱阴影。
- 蓝色只用于当前选中项、链接、键盘焦点与主操作；不使用渐变、玻璃效果、大面积品牌色块或卡片瀑布。
- 正文保持 14–15px，辅助信息 12–13px；课号、评分、数量等数字使用等宽数字。

## 版式规则

- 公开侧全宽列表使用表格；主从布局的窄栏索引允许使用两行数据行。两者都必须列对齐、整行可点并在悬停或选中时整行高亮；后台暂不纳入本轮收口，后续单独处理；
- 桌面端每屏可见至少 15 行数据；
- 筛选器是一条与表格同宽的紧凑工具栏，不独占竖向空间；
- 留白只用于分组，不用于气氛。

## 禁止

- 超过 32px 的展示型大标题；
- 装饰性图形、旋转元素、多层阴影；
- 任何为「气质」牺牲一行数据的排版。

## 低优先级 TODO

- 研究是否为任课关系展示“必抢”“避雷”等聚合标签。标签的数据来源、最低样本量、计算阈值、解释方式和最终措辞均未确定；首轮 UI 路线与原型不得依赖这些标签。
- 建立课程标签的权威来源与首批稳定学科标签。体育可由现有 `sports` 证据支持，数学、英语、计算机、经管等需另行策展；“专业课”依赖学生专业与培养方案，当前不得作为全局课程标签。
- 在浅色主题稳定后再评估深色主题；当前不得仅通过颜色反相生成未经验证的深色界面。

## 原型约束

- 首轮本地 UI 原型使用 Tailwind CSS 快速生成并比较结构变体。
- Tailwind 仅是原型工具，不构成生产技术选型；胜出方案需要按正式代码质量重写，生产是否采用 Tailwind 在组件边界确定后另行决定。
- 首轮路线与原型只覆盖学生侧的课评主界面、课程目录、任课关系详情、投稿问卷、目录补充申请及桌面/移动端；管理后台延后单独设计。
- 原型分两轮：第一轮只比较课评双栏主界面的 3 个结构变体；选出主界面方向后，第二轮再沿胜出语言制作课程目录与四段问卷。

## 首轮原型验收

- 使用模拟数据，只读运行，不连接写接口；一条 pnpm 命令可在本地启动。
- 同一路由提供 3 个结构差异明显的 Tailwind 变体，通过 `?variant=` 和底部浮动切换器切换，并支持左右方向键。
- 三个变体都遵守已确认的领域边界，但分别侧重最高扫描密度、最佳评价阅读和最强课程发现，供用户组合取舍。
- 桌面端必须呈现双栏主从结构；移动端必须呈现列表到详情的单栏导航。两种断点都要覆盖未选择、已选择、低样本、无评价和历史资料折叠状态。
- 任课关系必须以“教师 × 课程”呈现；不得出现教师全局总分、跨教师课程总评分、互动评论或依赖未确定标签的核心布局。
- 首轮不制作课程目录、问卷或后台的完整变体；这些页面在主界面方向确认后进入下一轮。

## 设计参考

- [CourseTable](https://coursetable.com/)：借鉴课程发现、紧凑筛选、数据层级、表格工具感和同屏主从浏览；不照搬 worksheet、课表规划或跨教师课程评价。
- [OMSCentral](https://www.omscentral.com/)：借鉴指标摘要、按学期近期优先的扁平评价流，以及单条评价中结构化指标与正文的组合；不照搬以课程为唯一评价对象的聚合方式，也不把历史资料混入正式评分。
- 任何参考都不得覆盖选课志的领域边界：评价对象始终是任课关系（教师 × 课程）；不产生跨教师课程总评分；历史评价独立展示；学生投稿只来自问卷并经过人工审核。
